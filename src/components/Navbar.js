export default function Navbar({username}){
    return (
        <header>
            <nav>
                <h1>
                TO DO LIST
                </h1>
            </nav>
            <span>{username}</span>
        </header>
    )
}
